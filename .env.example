# Root .env.example for docker-compose

JWT_SECRET=dev_secret_change_in_production_min_32_chars

# NATS WebSocket URL for frontend (used in docker-compose.yml)
# Note: Port 8080 is for WebSocket connections, port 4222 is for TCP connections
NEXT_PUBLIC_NATS_WS_URL=ws://localhost:8080